---
import BaseLayout from '../layouts/BaseLayout.astro';
import Landing from '../components/home/Landing.astro';
import NotesSidebar from '../components/notes/NotesSidebar.astro';
import SeriesExplorer from '../components/notes/SeriesExplorer.astro';
import { getPublishedNotes } from '../lib/notes';

const notes = await getPublishedNotes();
---

<BaseLayout>
  <Landing active="home">
    <Fragment slot="notes-left"><NotesSidebar notes={notes} /></Fragment>
    <Fragment slot="notes-main"><SeriesExplorer notes={notes} /></Fragment>
  </Landing>
</BaseLayout>
